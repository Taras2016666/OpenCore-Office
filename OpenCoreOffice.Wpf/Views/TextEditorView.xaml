<UserControl x:Class="OpenCoreOffice.Wpf.Views.TextEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="Auto" Width="Auto">
    <DockPanel>
        <ToolBar DockPanel.Dock="Top" Background="#E0E0E0">
            <Button Content="Open" Margin="2" Click="Open_Click"/>
            <Button Content="Save" Margin="2" Click="Save_Click"/>
            <Separator/>
            <Button Content="Bold" FontWeight="Bold" Margin="2"/>
            <Button Content="Italic" FontStyle="Italic" Margin="2"/>
            <Button Content="Underline" TextDecorations="Underline" Margin="2"/>
        </ToolBar>
        <TextBox x:Name="Editor"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 FontSize="16"
                 Margin="0,10,0,0"
                 Background="White"
                 BorderBrush="#CCCCCC"
                 BorderThickness="1"
                 Padding="8"/>
    </DockPanel>
</UserControl>